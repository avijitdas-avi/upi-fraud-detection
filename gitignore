# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Logs and temp files
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Note: data/*.csv and models/*.joblib are intentionally NOT ignored
# here — they're committed as project deliverables throughout the
# phases (generated datasets, trained models). Don't add them below
# without checking whether a later phase relies on them being tracked.
