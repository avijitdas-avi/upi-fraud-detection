# Copy this file to .env.local for local development, and set the
# same variable in your Vercel project's Environment Variables when
# deploying.

# URL of your FastAPI backend (Phase 7). Local default assumes it's
# running on your own machine via `uvicorn src.api.main:app --reload`.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
